services:
  ticket-scanner:
    build: .
    container_name: ticket-scanner
    restart: unless-stopped
    ports:
      - "3100:80"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - UPLOAD_DIR=/data/uploads
    volumes:
      - ticket-data:/data

volumes:
  ticket-data:
